{
    "scripts": {
        "cy:run:chrome": "cypress run -b Chrome",
        "cy:run:firefox": "cypress run -b Firefox"
    },
    "devDependencies": {
        "cypress-xpath": "^2.0.1",
        "mochawesome": "^7.1.3"
    }
}
